{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/"]
    },
    "coverage": {
      "dependsOn": ["^coverage"],
      "outputs": ["coverage/"]
    },
    "lint": {}
  }
}
